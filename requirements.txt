opencv-python>=4.8.0,<5.1
face-recognition>=1.3.0
numpy>=1.24.0,<3.0
python-dotenv>=1.0.0
psutil>=5.9.0
pytest>=7.0.0
setuptools>=70.0.0,<81.0.0

# dlib は face-recognition の依存。Python 3.12 / Ubuntu 24.04 では
# cmake, build-essential, libopenblas-dev が必要な場合があります:
#   sudo apt install cmake build-essential libopenblas-dev
#
# setuptools>=81 では pkg_resources が利用不可のため face_recognition_models が
# 動作しません。setuptools<81 を使用してください。
